{% for item in suricata_suppress_list %}
suppress gen_id {{ gen_id }}, sig_id {{ sig_id }}{% if item.track is defined %}, track {{ item.track }}, ip [{{ item.ip_addresses|join (',') }}]{% endif %}

{% endfor %}
